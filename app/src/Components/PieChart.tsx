import React from 'react';
import { Text, View } from 'react-native';

const PieChart: React.FC<{}> = () => {
  return (
    <View
      style={{
        height: 250,
        width: '100%',
        backgroundColor: 'gray',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <Text>CHAAAAAAAAART</Text>
    </View>
  );
};

export default PieChart;
